<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.accounting.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="Category">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="ledgerId" column="ledger_id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="isPreset" column="is_preset"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findById" resultMap="CategoryResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="findByUserIdAndType" resultMap="CategoryResultMap">
        SELECT * FROM category
        WHERE user_id = #{userId}
        <if test="type != null">AND type = #{type}</if>
        ORDER BY id
    </select>

    <select id="findByUserIdOrPreset" resultMap="CategoryResultMap">
        SELECT * FROM category
        WHERE (user_id = #{userId} OR is_preset = 1)
        <if test="type != null">AND type = #{type}</if>
        ORDER BY id
    </select>

    <select id="existsByUserIdAndNameAndType" resultType="boolean">
        SELECT COUNT(*) > 0 FROM category
        WHERE user_id = #{userId} AND name = #{name} AND type = #{type}
    </select>

    <select id="existsByUserIdAndLedgerIdAndNameAndType" resultType="boolean">
        SELECT COUNT(*) > 0 FROM category
        WHERE user_id = #{userId} AND ledger_id = #{ledgerId} AND name = #{name} AND type = #{type}
    </select>

    <select id="findByLedgerIdOrPreset" resultMap="CategoryResultMap">
        SELECT * FROM category
        WHERE (ledger_id = #{ledgerId} OR is_preset = 1)
        <if test="type != null">AND type = #{type}</if>
        ORDER BY id
    </select>

    <insert id="insert" parameterType="Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (user_id, ledger_id, name, type, is_preset, created_at)
        VALUES (#{userId}, #{ledgerId}, #{name}, #{type}, #{isPreset}, #{createdAt})
    </insert>

    <update id="update" parameterType="Category">
        UPDATE category
        SET name = #{name}, type = #{type}, ledger_id = #{ledgerId}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM category WHERE id = #{id}
    </delete>

</mapper>
